spring:
  datasource:
    url: jdbc:mysql://localhost:3306/AtoZ?useUnicode=true&characterEncoding=utf8&useSSL=false&jdbcCompliantTruncation=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
     name: Product

server:
  port: 8085
  

